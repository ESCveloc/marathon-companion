# Database
DATABASE_URL="postgresql://user:password@localhost:5432/marathon_companion"

# Bungie API (Phase 2 — API not public yet)
BUNGIE_API_KEY=""

# Note: Redis explicitly deferred per PRD. Use HTTP/CDN caching instead.
# Revisit only if Bungie API rate limits require server-side caching.
